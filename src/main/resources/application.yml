logging:
  level:
    org.springframework: DEBUG

server:
  port: 8080

spring:
  application:
    ui:
      title: 45Test
      mainScreenUrl: classpath:/templates/MainScreen.fxml

server-game-config:
  protocol: ${45_PROTOCOL:http}
  host: ${45_HOST:localhost}
  port: ${45_PORT:8080}
  url: ${server-game-config.protocol}://${server-game-config.host}:${server-game-config.port}

#Path to be added when the function is called depending on the action taken